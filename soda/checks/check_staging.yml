# Soda Checks for Staging Layer
# Validates transformed raw data in staging zone

checks for stg_comments:
  - row_count > 0:
      name: "Staging comments table has data"

checks for stg_embedded_movies:
  - row_count > 0:
      name: "Staging embedded movies table has data"

checks for stg_movie_cast:
  - row_count > 0:
      name: "DQ_STG_CAST_ROW_COUNT_GT_0"

checks for stg_movie_genres:
  - row_count > 0:
      name: "DQ_STG_GENRES_ROW_COUNT_GT_0"

checks for stg_movie_countries:
  - row_count > 0:
      name: "DQ_STG_COUNTRIES_ROW_COUNT_GT_0"

checks for stg_movie_directors:
  - row_count > 0:
      name: "DQ_STG_DIRECTORS_ROW_COUNT_GT_0"

checks for stg_movie_writers:
  - row_count > 0:
      name: "DQ_STG_WRITERS_ROW_COUNT_GT_0"

checks for stg_movie_languages:
  - row_count > 0:
      name: "DQ_STG_LANGUAGES_ROW_COUNT_GT_0"
